export default function OverviewPage() {
  return (
    <section className="mc-placeholder" aria-label="Overview placeholder">
      <p className="mc-placeholder-label">Overview</p>
      <h2>not delivered yet</h2>
      <p>
        This route contains the approved shell, navigation, environment identity,
        and reserved loading boundary only.
      </p>
    </section>
  );
}
